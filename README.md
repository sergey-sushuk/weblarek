# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.
##### Архитектура
```
Применяемый паттерн проектирования — MVP
``` 
#### Используемые данные и тыпы данных
 
```
IProduct
 
 id: интедификатор товара 
 description: описание товара
 image: изображение товара
 title: название товара 
 category: категория товара  
 price: number | null цена товара ( если она есть)
```
IBuyer
```
payment: спопоб оплаты 
 email: почта 
 phone: телефон
 address: адрес
```

##### Данные

Для учёта данных  классы разделяются по смыслу и зонам ответственности: ProductCatalog, Basket, Buyer

Класс ProductCatalog
```
Хранение товаров, которые можно купить.

Конструктор класса: constructor() - при инициализации по умолчанию принимает пустой массив.

Поля класса: arrayProducts: IProduct[] - хранит массив всех товаров. 
cardProduct: IProduct - хранит выбранный товар.

Метод для сохранения данных в классе: setArrayProducts(arrayProducts: IProduct[]): 
void - товар почученный в методе. 

getArrayProducts(): IProduct[] - массив товаров из модели.

getProduct(id: string): IProduct - получение товара по id.

setProductForDisplay(cardProduct: IProduct): void - сохранение товара для подробного отображения.

getProductForDisplay(id: string): IProduct - получение товара для подробного отображения.
```

Класс Basket

```
товары добавленные в корзину.

Конструктор класса: constructor() - при инициализации по умолчанию принимает пустой массив.

Поля класса: arrayProducts: IProduct[] - хранит массив товаров в корзине для  покупки.

Методы класса: getArrayBasket(): IProduct[] - получение массива товаров из корзины.

addProduct(product: IProduct): IProduct[] - добавление товара в корзину.
 
delProduct(id: string): void - удаление товара из корзины.
  
clearBasket(): void - очистка корзины. 

getTotalPrice(): number | null - получить стоимость всех выбранных товаров.
   
getItemsCount(): number - получить количество товаров в корзине.
    
 hasProduct(id: string): boolean - проверяет добавлен ли товар в корзину.
```

Класс Buyer
```
Методы класса: saveOrderData(buyerData: IBuyer ): void - сохранение данных покупателя в модели.

getBuyerData(): IBuyer  - получить данные пользователя. 

clearBuyerData(): void - очистить данные пользователя.

validationData(): boolean - валидация введенных данных.
```

### Слой коммуникации
Класс ApiComposition

```
получение данных с сервера и отправку данных на сервер c помощью Api.

Конструктор класса: constructor(instanceApi: IApi) - передает экземляр клазза который соответствует интерфейсу IApi.

Поля класса: instanceApi: IApi - экземпляр класса который соответствует интерфейсу IApi.

Методы класса: get<T extends object>(uri: string): Promise<T> - метод класса Api,  выполняет GET запрос на сервер и получает объект с массивом товаров.

post<T extends object>(uri: string, data: object, method?: ApiPostMethods): Promise<T> - метод класса Api,  принимает и отправляет на сервер объект с данными о пользователе, товаров которые он выбрал .
```

### Cлой предсталения 
 Класс CardView
 ```
 setTitle(text?: string): void
Устанавливает текст заголовка в titleEl. Если текст не предоставлен, устанавливает пустую строку.

this.setTitle('Название товара');
applyImage(src?: string, alt?: string): void
Применяет изображение к карточке. Обрабатывает случаи, когда src недействителен (пустой или содержит require).


setCategory(c?: string): void
Устанавливает категорию. Использует categoryLut для преобразования текста в CSS-слуг. Добавляет классы к categoryEl и устанавливает текст.



setPrice(price: Price | IProduct): void
Устанавливает цену. Обрабатывает Price или объект IProduct. Если цена null, отображает 'бесплатно' 

 ```

CartPanelWidget
```
Удаление товара: При клике на кнопку удаления товара (.basket__item-delete) вызывается коллбэк onRemove или эмитится событие 'basket/remove' с id товара.
Оформление заказа: При клике на кнопку оформления (.basket__button) вызывается коллбэк onCheckout или эмитится событие 'basket/checkout'.
```

CatalogGrid
```
При взаимодействии с карточкой товара вызывается коллбэк onOpen с id выбранного товара, позволяя открыть подробности или выполнить другие действия.

Компонент ищет контейнер .gallery в DOM при создании и выбрасывает ошибку, если элемент не найден.
Использует класс ProductTileWidget для рендеринга отдельных товаров.
Метод render обновляет DOM и возвращает корневой элемент для вставки на страницу.
```

CheckoutStageOne

```
это форма первого шага оформления заказа, позволяющая пользователю выбрать способ оплаты и ввести адрес доставки. Компонент расширяет базовый класс FormViewBase<State> и управляет состоянием формы, валидацией полей и взаимодействием с пользователем.


Взаимодействие с пользователем

Выбор способа оплаты: Кнопки оплаты (card и cash) переключают состояние оплаты. Активная кнопка подсвечивается.

Ввод адреса: Текстовое поле для адреса доставки обновляет состояние и запускает валидацию при каждом вводе.

Отправка формы: При клике на кнопку отправки выполняется валидация. Если ошибок нет, вызывается onNext с данными формы.

```

CheckoutStageTwo 

```
 это форма второго шага оформления заказа, предназначенная для ввода контактных данных пользователя: электронной почты и номера телефона. Компонент расширяет базовый класс FormViewBase<State>, управляет состоянием формы, выполняет валидацию введённых данных и обрабатывает отправку формы

 Ввод email: При изменении значения поля email обновляется состояние и запускается валидация.

Ввод телефона: Аналогично, при вводе телефона обновляется состояние и выполняется валидация.

Отправка формы: При нажатии кнопки отправки выполняется валидация. Если ошибок нет, вызывается onPay с введёнными данными.
 ```

 FormViewBase 
 ```
 абстрактный базовый класс для компонентов форм, расширяющий общий класс Component<TState>. Он инкапсулирует общую логику работы с HTML-формой, включая управление кнопкой отправки и отображение ошибок валидации.

 protected form: HTMLFormElement — DOM-элемент формы, с которым работает компонент.

protected submitBtn?: HTMLButtonElement — кнопка отправки формы (если найдена).

protected errorsEl?: HTMLElement — элемент для отображения сообщений об ошибках (если найден).
 ```

 HeaderBasketButton 
 ```
компонент кнопки корзины в шапке сайта, отображающий счётчик товаров в корзине и реагирующий на клики, чтобы открыть корзину.

Свойства
private root: HTMLButtonElement — корневой элемент кнопки корзины.

private counterEl: HTMLElement — элемент для отображения счётчика товаров.

private state: HeaderState — локальное состояние компонента с числом товаров в корзине.

private readonly events?: IEvents — объект для эмита событий (например, открытия корзины).

```

OverlayModal 
```
компонент модального окна с оверлеем, который управляет отображением содержимого, открытием и закрытием модального диалога. Обеспечивает закрытие по клику вне контента, по кнопке закрытия и по клавише Escape. При закрытии возвращает фокус на последний активный элемент.

Свойства
private overlay: HTMLElement — контейнер модального окна (оверлей).
private content: HTMLElement — контейнер для содержимого модального окна.
private closeBtn: HTMLButtonElement — кнопка закрытия модального окна.
private lastActive: HTMLElement | null — элемент, который был активен до открытия модального окна (для восстановления фокуса).
private readonly events?: IEvents — объект для эмита событий (например, уведомление о закрытии модального окна).
```

ProductModalWidget  

```
компонент карточки товара, отображаемый в модальном окне. Показывает информацию о товаре (название, описание, изображение, цену, категорию) и кнопку для добавления/удаления товара из корзины. Управляет состоянием наличия товара в корзине и уведомляет об изменениях через коллбэк.

Свойства
private id: string — уникальный идентификатор товара.
private price: Price — цена товара (тип Price предполагается определённым в коде).
private inBasket: boolean — флаг, указывающий, находится ли товар в корзине.
private btn: HTMLButtonElement — кнопка для добавления/удаления товара из корзины.
private props: ModalProps — данные товара, переданные при создании.
private onToggle: (id: string, inBasket: boolean) => void — коллбэк, вызываемый при переключении состояния товара в корзине.
```

ProductTileWidget
``` 
реализует элемент карточки товара в каталоге. Он наследуется от CardView и отображает заголовок, изображение/обложку, цену и категорию товара. При клике по карточке вызывается указанный обработчик открытия товара.Конструкторconstructor(private props: TileProps, private onOpen: (id: string) => void)props: TileProps — набор данных товара. Ожидается наличие следующих полей:id: string — идентификатор товараtitle: string — название товараimage?: string | HTMLImageElement — источник изображения или элемент изображенияprice?: string | number — цена товараcategory?: string — категория товараonOpen: (id: string) => void — колбэк, который вызывается при клике на карточку с передачей идентификатора товара
```

# Слой коммуникации

## Назначение

Слой коммуникации отвечает за организацию обмена данными и событиями между компонентами приложения, а также за взаимодействие с внешними сервисами и API. Его цель — обеспечить слабо связанную архитектуру, где компоненты взаимодействуют через централизованную систему событий и сообщений.

---

## Основные задачи слоя коммуникации

- Обработка и маршрутизация событий внутри приложения.
- Управление подписками и отписками на события.
- Взаимодействие с сервером для получения и отправки данных.
- Координация состояния приложения через событийную модель.
- Предоставление интерфейсов для обмена сообщениями между различными модулями и виджетами.

---

## Ключевые компоненты

### Интерфейс событий (IEvents)
```
Определяет набор методов для подписки на события, отписки и эмиссии событий с передачей данных. Позволяет компонентам регистрировать обработчики и уведомлять друг друга о произошедших действиях или изменениях.
```

### Событийный менеджер (EventBus)
```
Реализация интерфейса событий, обеспечивающая хранение подписчиков, вызов обработчиков при наступлении событий и управление жизненным циклом подписок. Позволяет централизованно управлять коммуникацией между частями приложения.
```
### API-клиенты и сервисы
```
Компоненты слоя коммуникации, ответственные за взаимодействие с внешними API — отправку запросов, получение ответов, обработку ошибок и трансформацию данных для последующего использования в приложении.

```

## Взаимодействие с UI-компонентами
```
- Компоненты подписываются на события, чтобы реагировать на изменения данных и пользовательские действия.
- При взаимодействии пользователя компоненты эмитят события, информируя другие части системы о произошедших изменениях.
- Слой коммуникации обеспечивает прозрачный обмен информацией, минимизируя прямые зависимости между компонентами.
```


## Рекомендации по использованию

```
- Использовать уникальные и осмысленные имена событий с разделением по категориям для удобства поддержки.
- Обязательно отписываться от событий при уничтожении компонентов для предотвращения утечек памяти.
- Обрабатывать ошибки при работе с асинхронными операциями и уведомлять об этом заинтересованные компоненты через события.
- Стремиться к чистой и понятной архитектуре, где коммуникация между модулями осуществляется только через слой коммуникации.

```
